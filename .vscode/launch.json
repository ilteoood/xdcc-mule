{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Node: attach",
            "type": "node",
            "request": "attach",
            "port": 9229,
            "address": "localhost",
            "localRoot": "${workspaceFolder}",
            "restart": true
        }
    ]
}